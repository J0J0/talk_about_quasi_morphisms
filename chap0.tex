\begin{thSetup} \label{setup}
    Sei $G$ stets eine Gruppe.
    Wir identifizieren $C^1(G;\R)$ (bzw. $\Cb^1(G;\R)$) mit dem Vektorraum aller
    reellwertigen (bzw. reellwertig beschränkten) Funktionen auf~$G$.
\end{thSetup}

